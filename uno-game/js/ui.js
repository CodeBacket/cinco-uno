"use strict";

/**
 * UI boundary.
 * DOM rendering and interactions are implemented in plan phase 02.
 */
function renderGame() {
  throw new Error("UI renderer not implemented yet.");
}

if (typeof module !== "undefined" && module.exports) {
  module.exports = {
    renderGame,
  };
}
