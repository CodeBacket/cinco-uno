"use strict";

/**
 * Placeholder bootstrap module for phase 01.
 * Setup flow and controller loop are implemented in plan 04.
 */
function bootstrap() {
  return null;
}

if (typeof window !== "undefined") {
  window.addEventListener("DOMContentLoaded", bootstrap);
}

exports.bootstrap = bootstrap;
